---
# Recreate rather than restart: a changed compose file means new server flags,
# and restarting the existing container would keep the old ones.
- name: Restart llama
  ansible.builtin.command:
    cmd: docker compose up -d --force-recreate
    chdir: "{{ llama_install_dir }}"
  changed_when: true
